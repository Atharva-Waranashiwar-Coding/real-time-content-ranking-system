# Screenshot Capture Plan

This repository does not check in rendered screenshots by default, but these are the recommended captures for a portfolio case study or README refresh.

## Suggested Captures

### 1. Feed Overview

- Route: `/feed`
- User: `alice_dev`
- State: feed loaded, top card selected, score breakdown drawer open
- Why it matters: shows explainable ranking, content cards, and interaction controls in one frame

### 2. Insights View

- Route: `/insights`
- User: `charlie_sysadmin`
- State: topic affinity chart and trending content chart both visible
- Why it matters: connects user interests, feed outputs, and content analytics

### 3. Experiment Dashboard

- Route: `/experiments`
- User: `dana_ml` or `emma_fullstack`
- State: comparison chart visible with non-zero outcome metrics
- Why it matters: shows deterministic assignment plus attributed experiment results

### 4. Operations Dashboard

- Surface: Grafana `Ranking and Feed Operations`
- State: recent demo traffic present after a few feed loads and interactions
- Why it matters: demonstrates operational maturity instead of only product UI polish

## Naming Suggestion

If you add images to this directory later, use:

- `feed-overview.png`
- `insights-view.png`
- `experiment-dashboard.png`
- `grafana-ranking-ops.png`
